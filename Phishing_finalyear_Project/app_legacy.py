# This file is legacy and not used in the Django project
# The Django application is managed through manage.py and uses detector/views.py
# To run the Django application, use: python manage.py runserver
#
# This Flask alternative can be used if you prefer Flask over Django
# To use this, you would need to:
# 1. Install Flask: pip install flask flask-cors
# 2. Update imports in train_model.py to use Flask
# 3. Rename this file from app_legacy.py to app.py
# 4. Run: python app.py
